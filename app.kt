fun main() {
    repeat(readln().toInt()) {
        var n = readln().toInt()
        if (n and 1) {
            println("NO")
        } else {
            var arr = readln().split(" ").map { it.toInt() }.sorted()
            var j=0
            for(i in n/2..n-1)
            {
                if()
            }
        }
    }
}

#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef vector<int> vi;
typedef pair<int,int> pii;

#define pb emplace_back
#define mp make_pair
#define F first
#define S second

signed main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    ll n,q;
    cin>>n>>q;
    vector<char> v;
    int i=0;
        for(int j=97;j<97+q;++j)
        {
            cout<<char(j);
            i++;
            if(i==n) exit(0);
            cout<<char()
            if(j==97+q-1) j=96;
        }
}